[build]
builder = "NIXPACKS"
buildCommand = "cd tiktok-ads-integration/webhook-server && npm install"

[deploy]
startCommand = "cd tiktok-ads-integration/webhook-server && node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
